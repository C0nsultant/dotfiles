- clean: ['~']

- create:
    - ~/.zsh

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.profile: profile
    ~/.zshrc: zshrc

- shell:
    - [curl -L git.io/antigen > ~/.zsh/antigen.zsh]

